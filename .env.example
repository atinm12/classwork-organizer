# Canvas personal access token (Account > Settings > New Access Token).
# Never commit the real value — set it in .env.local locally and in Vercel for production.
CANVAS_API_TOKEN=

# Canvas instance base URL. Defaults to https://canvas.cmu.edu if unset.
CANVAS_BASE_URL=https://canvas.cmu.edu
